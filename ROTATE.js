
Client.waitTick(2)
Chat.say("SMOKE WEED EVERYDAY")
Client.waitTick(2)
Chat.title("ROTATING", "MLG", 15, 22, 66);
const loop_count = 500;
for(let i = 0; i < loop_count; ++i) {
//let blockstate = Player.rayTraceBlock(666, true).getName()//.getBlockState()
//Chat.log(blockstate.toString())
Chat.say(".rotation add 0 40")
Client.waitTick(2) ;

}
